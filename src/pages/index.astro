---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SERVICES } from '../data/services';
import { SITE } from '../data/site';
---

<BaseLayout title="Luke Hollenback — Product, engineering, and AI advisory" description={SITE.description}>
  <main id="main" class="home">
    <div class="home-inner split">
      <div>
        <p class="availability">{SITE.tagline}</p>
        <h1>{SITE.statement}</h1>
        <p class="lede">
          Twelve years building hard software that powers real businesses. I now run product development for an
          enterprise AI platform. Multiple patents filed, countless products shipped, and an addiction to simplicity.
        </p>
        <div class="action-row">
          <div class="action-row-items">
            <a class="button" href="/contact">Start a Conversation <span class="button-arrow">→</span></a>
            <a class="quiet-link" href="/bio">Check my Experience</a>
          </div>
        </div>
      </div>
      <div class="hairline-top">
        {
          SERVICES.map((service) => (
            <a class="service hairline-bottom" href="/services">
              <span class="service-name">{service.name}</span>
              <span class="service-tagline">{service.tagline}</span>
            </a>
          ))
        }
      </div>
    </div>
  </main>
</BaseLayout>

<style>
  .home {
    flex: 1;
    display: flex;
    align-items: center;
    padding: clamp(18px, 4vh, 56px) var(--shell-pad);
  }

  .home-inner {
    --split-min: 340px;
    width: 100%;
    max-width: 1180px;
    margin: 0 auto;
    gap: clamp(20px, 5vw, 72px);
    align-items: end;
  }

  .availability {
    margin: 0 0 clamp(10px, 3vh, 26px);
    font-size: 12.5px;
    letter-spacing: 0.02em;
    color: var(--link);
  }

  h1 {
    max-width: 13.5em;
    margin: 0 0 clamp(10px, 2.2vh, 24px);
    font-size: clamp(30px, min(5.6vw, 7.2vh), 74px);
    font-weight: 600;
    line-height: 1.04;
    letter-spacing: -0.032em;
  }

  .lede {
    max-width: 44ch;
    margin: 0 0 clamp(14px, 3.2vh, 34px);
    font-size: clamp(13.5px, min(1.5vw, 2.5vh), 18px);
    line-height: 1.5;
    color: var(--body-soft);
  }

  /* 380px is where this button and link stop fitting on one line. The nav already offers Bio. */
  @container (width < 380px) {
    .quiet-link {
      display: none;
    }
  }

  /* Below 800px the grid is one column and the list ends just above the footer's hairline. */
  @media (width < 800px) {
    .service:last-child {
      background-image: none;
    }
  }

  .service {
    display: block;
    padding: clamp(9px, 1.4vh, 13px) 0;
    color: var(--ink);
  }

  .service:hover {
    color: var(--link);
  }

  .service-name {
    font-size: clamp(15.5px, min(1.9vw, 2.9vh), 22px);
    letter-spacing: -0.02em;
  }

  .service-tagline {
    display: block;
    margin-top: 3px;
    font-size: 12.5px;
    letter-spacing: 0.01em;
    color: var(--muted);
  }
</style>
